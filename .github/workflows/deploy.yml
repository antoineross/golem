name: Deploy to GCE

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    name: Test deploy workflow
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploy workflow works"
